public class java11if3 {
    public static void main(String[] args) {
        String x = 33 < 10 ? "ciao" : "addio";  // ? se è vero CIAO | : altrimenti ADDIO

        System.out.println(x);

        /* TERNARY OPERATOR molto pratico se si vuole una variabile con un valore piuttosto che un altro
        ALTRIMENTI CON IF SAREBBE + LUNGO
        if() {
             x = "ciao"
        }else
             x = "addio"

        System.out.println(x);
         */
    }
}